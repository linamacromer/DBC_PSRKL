  <div class="competitors-header">
    <h1>Competitors:</h1>

    <%= link_to "Add new competitor", new_competitor_path(@competitors), class: "btn btn-primary" %>
  </div>

  <ul class="list-group competitors-list">
    <% @competitors.each do |competitor| %>
      <li class="list-group-item"><%= competitor.name %></li>
    <% end %>
  </ul>
